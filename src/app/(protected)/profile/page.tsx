export default function ProfilePage() {
  return (
    <div>
      <h1 className="text-xl font-semibold text-gray-900 mb-6">Профіль</h1>
      {/* ProfileCard + NotificationSettingsForm — leads slice */}
      <p className="text-sm text-gray-400">ProfileCard — буде реалізовано</p>
    </div>
  );
}
